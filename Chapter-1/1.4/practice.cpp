#include <iostream>

using namespace std;

int main()
{
    // int x;
    // int y,z;

    // int width;
    // width = 5;

    // cout << "\n";
    // cout << width;

    // width = 7;
    // cout << "\n";
    // cout << width;

    // int width {5};
    // cout << width;
    // return 0;

    // int a;     // no initializer (default initialization)
    // int b = 5; // initial value after equals sign (copy initialization)
    // int c(6);  // initial value in parenthesis (direct initialization)

    // List initialization methods (C++11) (preferred)
    // int d{7};    // initial value in braces (direct list initialization)
    // int e = {8}; // initial value in braces after equals sign (copy list initialization)
    // int f{};     // initializer is empty braces (value initialization)

    // int width{}; // value initialization / zero initialization to value 0
    // cout << width;

    // int x{0};  // explicit initialization to value 0
    // cout << x; // we're using that zero value

    // int x{};       // value initialization
    // cin >> x; // we're immediately replacing that value

    // int a = 5, b = 6;      // copy initialization
    // int c(7), d(8);        // direct initialization
    // int e{9}, f{10};       // direct brace initialization
    // int g = {9}, h = {10}; // copy brace initialization
    // int i{}, j{};          // value initialization

    // int p,q = 5; // Wrong (p is not initialized)
    // int p = 5, q = 5;

    // int p, q(5);
    // int p, q{5};

    // [[maybe_unused]] double pi{3.14159};
    // [[maybe_unused]] double gravity{9.8};
    // [[maybe_unused]] double phi{1.61803};

    // cout << pi << '\n';
    // cout << phi << '\n';

    // return 0;
}